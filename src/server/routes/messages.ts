// Rutas de mensajes para TheFreed.v1
import { Router } from 'express';

const router = Router();

// TODO: Implementar rutas de mensajes
// - GET /api/messages/conversations - Conversaciones
// - GET /api/messages/conversation/:id - Mensajes de una conversación
// - POST /api/messages/send - Enviar mensaje
// - PUT /api/messages/:id/read - Marcar como leído

export default router;