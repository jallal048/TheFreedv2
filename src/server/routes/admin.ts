// Rutas de administración para TheFreed.v1
import { Router } from 'express';

const router = Router();

// TODO: Implementar rutas de administración
// - GET /api/admin/dashboard - Dashboard de admin
// - GET /api/admin/users - Gestión de usuarios
// - PUT /api/admin/users/:id/suspend - Suspender usuario
// - GET /api/admin/analytics - Analytics generales
// - GET /api/admin/reports - Gestión de reportes

export default router;