// Rutas de moderación para TheFreed.v1
import { Router } from 'express';

const router = Router();

// TODO: Implementar rutas de moderación
// - POST /api/moderation/report - Reportar contenido/usuario
// - GET /api/moderation/reports - Ver reportes (admin)
// - PUT /api/moderation/reports/:id/resolve - Resolver reporte
// - GET /api/moderation/appeals - Ver apelaciones

export default router;